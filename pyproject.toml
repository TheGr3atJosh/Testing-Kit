[project]
name = "adaptix-testing"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "paramiko>=4.0.0",
    "pyyaml>=6.0.3",
    "requests>=2.33.1",
    "rich>=14.2.0",
]

[project.scripts]
adaptix-testing = "run:main"
